import java.util.*;

class userlist {
  String[] userlist;
  String email;
  String username;
  static <T> void getUser(T user) {
    String[] userlist;
    if (user == "" || user == null) {
      System.exit(0);
    }
    Scanner user = new Scanner();
    user.

    }
  }
