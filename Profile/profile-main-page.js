let enrolledCourses = {};

let validCourses = ["JavaScript", "Python", "C#", "Java", "C++", "C", "TypeScript"];
function AddEnrolledCourses (courseToEnroll) {
    if (courseToEnroll != validCourses) {

    }
    enrolledCourses.add(courseToEnroll);
}