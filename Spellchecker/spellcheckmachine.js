const dictUrl = 'words_alpha.txt';
fetch(dictURL).then(r = r.text());

let r = textInput;
fetch(dictURL).then(function y() {
  if (r = dictUrl) {
    document.getElementById('comments').innerHTML = 'true';
  }
});


